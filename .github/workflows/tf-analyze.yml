name: tf-analyze

on:
  pull_request:
    paths:
      - 'terraform/**'
      - '.github/workflows/tf-analyze.yml'
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # belt-and-braces; v0.2.6+ auto-fetches the base ref for --mode diff

      - uses: ChrisAdkin8/tf-analyze@v1
        with:
          fail-on: HIGH
          mode: static                         # demo PRs show the full fixture's findings, not only what changed (see issue #19)
          post-pr-comment: true                # inline `suggestion` blocks on every PR
          compliance-framework: owasp_iac      # optional — adds a collapsible compliance gap report
          attack-graph: true                   # optional — embeds Mermaid attack graph in PR summary
          ref: v0.2.6
